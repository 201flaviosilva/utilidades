import { randomString } from "./src/main";

// TODO: use regex to check :)

describe("randomString.js", () => {
	test("the return string length", () => {
		// expect(randomString().length).toBe(12);
	});
});
